Create a new dose

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, as: :select %>
  <%= f.input :description %>
  <%= f.submit "Add ingredient" %>
<% end %>
<br>
<%= link_to "Add new ingredient to list", new_ingredient_path %>
